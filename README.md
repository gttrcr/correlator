# correlator

## setup
Launch ```./setup.sh platform``` where ```platform``` is:
* ```w``` for windows
* ```l```for linux

## build
Launch  ```./build.sh```. It build for every supported platform

## release
Launch ```./release.sh```. It release every supported platform
