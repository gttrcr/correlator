def func11(domain, a1, a2):
    return a1 + a2 * domain[0]


def func12(domain, a1, a2, a3):
    return a1 + a2 * domain[0] + a3 * domain[0] ** 2


def func13(domain, a1, a2, a3, a4):
    return a1 + a2 * domain[0] + a3 * domain[0] ** 2 + a4 * domain[0] ** 3


def func14(domain, a1, a2, a3, a4, a5):
    return (
        a1
        + a2 * domain[0]
        + a3 * domain[0] ** 2
        + a4 * domain[0] ** 3
        + a5 * domain[0] ** 4
    )


def func15(domain, a1, a2, a3, a4, a5, a6):
    return (
        a1
        + a2 * domain[0]
        + a3 * domain[0] ** 2
        + a4 * domain[0] ** 3
        + a5 * domain[0] ** 4
        + a6 * domain[0] ** 5
    )


def func16(domain, a1, a2, a3, a4, a5, a6, a7):
    return (
        a1
        + a2 * domain[0]
        + a3 * domain[0] ** 2
        + a4 * domain[0] ** 3
        + a5 * domain[0] ** 4
        + a6 * domain[0] ** 5
        + a7 * domain[0] ** 6
    )


def func17(domain, a1, a2, a3, a4, a5, a6, a7, a8):
    return (
        a1
        + a2 * domain[0]
        + a3 * domain[0] ** 2
        + a4 * domain[0] ** 3
        + a5 * domain[0] ** 4
        + a6 * domain[0] ** 5
        + a7 * domain[0] ** 6
        + a8 * domain[0] ** 7
    )


def func18(domain, a1, a2, a3, a4, a5, a6, a7, a8, a9):
    return (
        a1
        + a2 * domain[0]
        + a3 * domain[0] ** 2
        + a4 * domain[0] ** 3
        + a5 * domain[0] ** 4
        + a6 * domain[0] ** 5
        + a7 * domain[0] ** 6
        + a8 * domain[0] ** 7
        + a9 * domain[0] ** 8
    )


def func19(domain, a1, a2, a3, a4, a5, a6, a7, a8, a9, a10):
    return (
        a1
        + a2 * domain[0]
        + a3 * domain[0] ** 2
        + a4 * domain[0] ** 3
        + a5 * domain[0] ** 4
        + a6 * domain[0] ** 5
        + a7 * domain[0] ** 6
        + a8 * domain[0] ** 7
        + a9 * domain[0] ** 8
        + a10 * domain[0] ** 9
    )


def func110(domain, a1, a2, a3, a4, a5, a6, a7, a8, a9, a10, a11):
    return (
        a1
        + a2 * domain[0]
        + a3 * domain[0] ** 2
        + a4 * domain[0] ** 3
        + a5 * domain[0] ** 4
        + a6 * domain[0] ** 5
        + a7 * domain[0] ** 6
        + a8 * domain[0] ** 7
        + a9 * domain[0] ** 8
        + a10 * domain[0] ** 9
        + a11 * domain[0] ** 10
    )
